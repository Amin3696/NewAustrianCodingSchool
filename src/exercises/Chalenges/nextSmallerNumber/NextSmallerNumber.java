package exercises.Chalenges.nextSmallerNumber;

import java.util.Arrays;
import java.util.List;
import java.util.Scanner;

public class NextSmallerNumber {

  /*  private Scanner scanner = new Scanner(System.in);

    public String getNumber() {
        System.out.println("please enter a positive Integer!");
        String input = scanner.next();
        String firstCheck=checkLength(input);
       String secondCheck= checkLastDigit(firstCheck);
        replaceLastDigit();
        String result = concatenateDigits();


        return result;
    }

    private String checkLength(String input) {

        int length = input.length();
        if (length < 2 ) {
            return "-1";
        }
        return input;
    }

    private String checkLastDigit(String firstCheck) {
        String[] split=firstCheck.split("");
        List<String> digits = Arrays.asList(split);
        int length=firstCheck.length();
        for (int digit = length; digit <length-1 ; digit--) {
            String lastDigit = digits.get(length - 1);

        }

        return checkIfTheLastValuIsbiggerThanAllOthers(firstCheck);
    }*/
}

